# Previous iterations of the project used Yarn-specific settings in this
# `.npmrc`. They triggered npm warnings, so we intentionally keep the file
# informational-only to avoid noisy output during npm scripts.
